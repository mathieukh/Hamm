import {
  INFURA_PROVIDER_API_KEY,
  activeChain,
  supportedChains,
} from "@/config";

export const thirdwebConfig = {
  infuraApiKey: INFURA_PROVIDER_API_KEY,
  activeChain,
  supportedChains,
};
