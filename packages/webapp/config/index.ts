import {
	CONTRACT_ADDRESSES,
	INFURA_PROVIDER_API_KEY,
	WALLET_CONNECT_PROJECT_ID,
} from "./env";
import { activeChain, supportedChains } from "./web3";

export {
	INFURA_PROVIDER_API_KEY,
	WALLET_CONNECT_PROJECT_ID,
	CONTRACT_ADDRESSES,
	activeChain,
	supportedChains,
};
